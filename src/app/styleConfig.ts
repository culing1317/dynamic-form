/**
 * itmeMargin： 每一行margin
 * leftParmargin: 每一行lable部分margin
 * leftParMaxWid： 每一行lable部分最大宽度
 * rightParMaxWid： 每一行input部分最大宽度
 * increaseDisplay: 子group中表单是否要在一行显示。
 *                  flex: 一行显示;默认换行显示
 */
export enum BaseStyle {
    ItmeMargin = '--itmeMargin',
    LeftParmargin = '--leftParmargin',
    LeftParMaxWid = '--leftParMaxWid',
    RightParMaxWid = '--rightParMaxWid',
    IncreaseDisplay = '--increaseDisplay',
}
